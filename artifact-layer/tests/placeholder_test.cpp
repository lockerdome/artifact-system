#include <gtest/gtest.h>

// Placeholder test to prove the build and test infrastructure works.
// This will be replaced by real tests in subsequent phases.

TEST(PlaceholderTest, BuildWorks) {
  EXPECT_EQ(1 + 1, 2);
}
