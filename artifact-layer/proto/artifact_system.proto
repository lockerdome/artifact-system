syntax = "proto3";

package artifact_system;

// Placeholder for system proto definitions.
// P1 will add: IndexDefinition, ReferenceOption, TypeDefinition,
// TypeVersionDefinition, and related messages.

// Minimal message so the proto compiles.
message Placeholder {
  string name = 1;
}
